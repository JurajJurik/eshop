<div>
    @if ($cartSize)
        <div class="border border-red-600 text-beige bg-red-600 rounded-full size-5 flex items-center justify-center p-0.5 text-xs absolute -right-4 -top-4">{{ $cartSize }}</div>
    @endif
</div>
